// use esp_hal::gpio::{Output, OutputPin};

// pub const SAFETY_LIGHT_PIN_HANDLE: u8 = 10;
// pub static SAFETY_LIGHT_HANDLE: Mutex<
// RefCell<
//     Option<Output<OutputPin<SAFETY_LIGHT_PIN_NUMBER>>>,
// >,
// > = Mutex::new(RefCell::new(None));

// pub fn set_safety_light(state: bool) {
//     critical_section::with(|cs| {
        
//     });
// } 